export const config = {
  scrollSpeed: 0, //current, time-based multiplier (unitless)
  targetScrollSpeed: 0, // target mulitplier
  accelPerSec: 8, // how fast to approach target (units/sec)
  maxScrollSpeed: 10, //comment out
  // globalFrameCount = 0,
  butterfliesSpawned: false,
  shakingTimeStart: null,
  lastTime: 0,
};
